uses crt;
var arr: array [1..100] of integer;
    n, i, s, j: integer;
begin
    clrscr;
    write('Nhap so: ');
    readln(n);
    for i:= 1 to n do
        begin
            for j:= 2 to round(sqrt(i)) do
                
        end;
end.